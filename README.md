# SkillRadar: Мониторинг востребованных навыков

Проект автоматически собирает вакансии с HH.ru, анализирует требования и показывает, какие технологии сейчас нужны рынку (SQL, Python, Docker и т.д.).

## Как это работает
Данные проходят путь от источника до аналитической базы без участия человека.



1.  **Airflow** (Оркестратор): Раз в сутки запускает скрипт сбора.
2.  **HeadHunter API** (Источник): Отдает сырые данные о вакансиях.
3.  **Kafka** (Буфер): Гарантирует, что данные не потеряются при сбоях базы.
4.  **ClickHouse** (DWH): Хранит массивы данных для быстрой аналитики.
5.  **Metabase** (Витрина): Строит графики и дашборды (в процессе настройки).

## Стек технологий
* **Сбор:** Python 3.9, Requests.
* **Инфраструктура:** Docker, Docker Compose.
* **ETL:** Apache Airflow, Apache Kafka + Zookeeper.
* **Storage:** ClickHouse.

## Быстрый старт
Требуется установленный Docker Desktop.

1.  **Склонировать репозиторий:**
    ```bash
    git clone [https://github.com/TVOY_NICK/skill_radar.git](https://github.com/TVOY_NICK/skill_radar.git)
    ```
2.  **Запустить сервисы:**
    ```bash
    docker-compose up -d
    ```
3.  **Подготовить базу (только первый раз):**
    ```bash
    cat schema.sql | docker exec -i clickhouse clickhouse-client --multiquery
    ```
4.  **Запустить сбор:**
    Открыть `http://localhost:8081` (логин/пароль: admin/admin) и включить DAG `hh_vacancy_parser`.

## Статус проекта
* ✅ Инфраструктура развернута (6 контейнеров).
* ✅ Сбор данных работает и пишет в Kafka.
* ✅ ClickHouse получает данные из Kafka.
* 🚧 Настройка BI-системы (Metabase).